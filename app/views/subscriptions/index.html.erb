<% @customer.subscriptions.each do |s| %>
  Start: <%= s.current_period_start %>
  End: <%= s.current_period_end %>
  Plan: <%#= @plans %> #TODO: show the plan info 
  <%= link_to "Cancel now", subscription_path(s.id), :method => :delete, :data => {:confirm => 'Are you sure?'} %>
  <br>
<% end %>
